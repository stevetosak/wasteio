services:
  device:
    build: .
    environment:
      SIM_MODE: "true"
      DATA_DIR: /app/data
    volumes:
      - device-data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  device-data:
